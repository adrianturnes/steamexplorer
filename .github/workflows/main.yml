name: Master

on:
  push:
    branches: [ master ]

jobs:
  phpunit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: build
        run: make install

      - name: composer
        run: make composer

      - name: unit tests
        run: make test-unit
